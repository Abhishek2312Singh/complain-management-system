import React from "react";

const PendingRequests = () => {
  return (
    <section>
      <h2>Pending Request</h2>
      <p style={{ marginBottom: "12px", color: "#6b7280", fontSize: "0.9rem" }}>
        This is a placeholder for pending complaints. You can connect this component to the backend
        API that returns all pending complaints and render them in a table here.
      </p>
    </section>
  );
};

export default PendingRequests;


